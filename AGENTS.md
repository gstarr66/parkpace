# Expo SDK

This project is pinned to Expo SDK ~54.0.37 to match the Expo Go client version
available on the App Store / Play Store. Read the versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Do not upgrade the Expo SDK without checking with Gary first — a newer SDK
would require a development build instead of Expo Go.
